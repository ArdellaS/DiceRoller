function random4(min, max) {
    min = Math.ceil(1);
    max = Math.floor(5);
    document.getElementById("display").innerHTML = Math.floor(Math.random() * (max - min)) + min;
  }

  function random6(min, max) {
    min = Math.ceil(1);
    max = Math.floor(7);
    document.getElementById("display").innerHTML = Math.floor(Math.random() * (max - min)) + min;
  }

  function random8(min, max) {
    min = Math.ceil(1);
    max = Math.floor(9);
    document.getElementById("display").innerHTML = Math.floor(Math.random() * (max - min)) + min;
  }

  function random10(min, max) {
    min = Math.ceil(1);
    max = Math.floor(11);
    document.getElementById("display").innerHTML = Math.floor(Math.random() * (max - min)) + min;
  }

  function random12(min, max) {
    min = Math.ceil(1);
    max = Math.floor(13);
    document.getElementById("display").innerHTML = Math.floor(Math.random() * (max - min)) + min;
  }

  function random20(min, max) {
    min = Math.ceil(1);
    max = Math.floor(21);
    document.getElementById("display").innerHTML = Math.floor(Math.random() * (max - min)) + min;
  }
